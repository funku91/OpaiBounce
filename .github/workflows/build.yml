name: Build

on:
  push:
    branches: [ main, master, legacy ]
  pull_request:
    branches: [ main, master, legacy ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 8
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '8'
          cache: 'gradle'

      - name: Grant execute permission
        run: chmod +x gradlew

      - name: Build with Gradle
        run: |
          ./gradlew --stacktrace --info setupDevWorkspace build || \
          ./gradlew --stacktrace --refresh-dependencies setupDevWorkspace build
        env:
          GRADLE_OPTS: "-Dorg.gradle.daemon=false"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: OpaiBounce-Build
          path: build/libs/
